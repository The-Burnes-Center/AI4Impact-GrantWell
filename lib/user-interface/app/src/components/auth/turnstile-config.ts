// Injected by vite `define`. Empty means Turnstile is not configured for this build, and every
// call site degrades to its pre-Turnstile behaviour.
export const TURNSTILE_SITE_KEY = __TURNSTILE_SITE_KEY__;

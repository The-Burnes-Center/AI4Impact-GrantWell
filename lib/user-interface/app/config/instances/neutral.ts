import { Branding, defaultBranding } from "../../src/common/branding";

// Neutral core — no instance identity. A delivered repo replaces this selection with its own.
export const branding: Branding = defaultBranding;

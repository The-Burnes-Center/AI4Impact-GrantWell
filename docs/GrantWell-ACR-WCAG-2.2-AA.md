# GrantWell Accessibility Conformance Report

**WCAG Edition — Based on VPAT® Version 2.5**

| | |
|---|---|
| **Name of Product/Version** | GrantWell (web application), v1.4 |
| **Product Description** | AI-assisted grant discovery and application-drafting tool for municipalities: grant (NOFO) browsing and search, requirements checklists, AI chat assistant, guided document editor, and an administrative dashboard. |
| **Report Date** | July 10, 2026 (remediation applied and re-verified same day; see Remediation Status) |
| **Contact Information** | Anjith Prakash, Burnes Center for Social Change / AI for Impact — chathankandy.a@ai4impact.ai |
| **Evaluation Methods Used** | (1) Static source-code review of the full frontend SPA (`lib/user-interface/app/src`, React 18 + Vite, ~137 source files) across five dimensions: document structure/semantics, forms and input assistance, keyboard operability and focus, non-text content/color/visual presentation (contrast ratios computed with the WCAG relative-luminance formula), and dynamic content/status messages/rich-widget ARIA — every finding cited to file and line. (2) Automated **axe-core scan** (WCAG 2.0/2.1/2.2 A+AA rulesets, via Playwright + headless Chromium at 1280×900) of the running application connected to the staging backend, covering 16 page states: landing, login, home (before/after grant selection), requirements checklist, chat playground (help dialog, empty, and live-streamed conversation), chat sessions, document-editor drafts and editor, and all four admin dashboard tabs. (3) A screen-reader-oriented pass using the browser accessibility tree (ARIA snapshots per page), programmatic keyboard tab-walks recording focus visibility (computed outline/box-shadow) per stop, and live-region observation during chat streaming. (4) An **NVDA screen-reader pass** (guidepup-automated NVDA on Windows 11 + Microsoft Edge, capturing NVDA's actual spoken output) covering all nine user-reachable pages: unauthenticated landing, sign-in (page read plus a full sign-in performed through NVDA), authenticated home (page read and keyboard tab-walk), requirements checklist, chat playground with a live assistant conversation, chat sessions, document editor (welcome modal, entry screen, and wizard step 1), drafts, and admin dashboard. Deep states were also NVDA-tested: the maintenance page (with the maintenance flag temporarily enabled in staging), the session delete-confirmation modal, wizard steps 2-3, the section-editor stepper, and the dashboard Grants tab. |
| **Applicable Standards/Guidelines** | [WCAG 2.2](https://www.w3.org/TR/WCAG22/), Level A (Table 1) and Level AA (Table 2) |
| **Evaluator** | Anjith Prakash (Burnes Center for Social Change / AI for Impact), with AI-assisted auditing and test automation performed by Anthropic's Claude Fable 5 (Claude Code) |

## Notes

1. **Testing coverage.** This report combines a full code audit, an axe-core scan (zero violations across all 16 tested page states), an accessibility-tree/keyboard pass, and an NVDA pass with captured speech output covering all nine user-reachable pages of the running application plus deep states.
2. **Scope**: the deployed web frontend, both the unauthenticated marketing/landing/login pages and the authenticated application (home, requirements checklist, chat, document editor, sessions, admin dashboard). Out of scope: PDF/DOCX exports generated via jsPDF/docx, email content sent by Cognito, and AWS-hosted backend services.
3. **Content-dependent caveat**: NOFO summaries and AI chat responses render author/AI-supplied markdown; conformance of that dynamic content (e.g., link text quality, language of parts) depends on the content itself.
4. WCAG 2.2 removed Success Criterion 4.1.1 Parsing; it is intentionally absent from Table 1.
5. One finding was also a functional product bug affecting all users equally: the toast-notification system (`NotificationProvider`/`NotificationBar`) was only mounted in the unused `layouts/BaseAppLayout.tsx`, so all chat and dashboard toast notifications were silently dropped. **Remediated 2026-07-10** — the providers are now mounted at the app root and the fix was verified live.
6. **Remediation status**: every item in the Remediation Priorities list below was applied to the source tree on July 10, 2026 and re-verified against a local build (9/9 automated checks passed: keyboard grant selection end-to-end, notification announcement, focus visibility, input-border contrast, login title, and clean axe re-scans of home and dashboard). **The fixes are not yet deployed** — the deployed environments continue to reflect the pre-remediation findings until the frontend is rebuilt and released.

## Automated and Live Testing Results

Conducted July 10, 2026 against a local build (`vite dev`) connected to the staging backend (grantwell.us environment), using a temporary admin/developer test account (created for the scan, deleted afterward). axe-core with the `wcag2a`, `wcag2aa`, `wcag21a`, `wcag21aa`, `wcag22aa` rule tags.

**axe-core: 0 violations in all 16 tested page states** — landing, login, home (empty and with a grant selected), requirements checklist, chat playground (auto-opened help dialog, empty session, and a live conversation with a streamed AI reply), chat sessions, document-editor drafts, document editor, and the four admin dashboard tabs (Grants, Processing Review, User Management, Feature Rollouts). 42 "incomplete" color-contrast items on the home hero (axe could not compute backgrounds behind decorative pseudo-elements) were manually verified as passing (dark #282828-family text on light backgrounds, > 7:1).

Live confirmations of code-review findings:

- **Grant rows unreachable by keyboard (2.1.1 blocker)** — a full tab-walk of the home page goes from the sort-header buttons directly to pagination; no grant row ever receives focus.
- **Notifications never appear (4.1.3)** — clicking the dashboard's Refresh button (whose code calls `addNotification("success", "Dashboard refreshed successfully")`) produced zero `role="alert"`/`role="status"` elements and zero toast DOM nodes.
- **Send/Stop focus invisible (2.4.7)** — with keyboard focus on the chat Send button (and on Stop during streaming), computed styles show `outline: none` and no box-shadow.
- Working as intended: sign-in flow fully keyboard operable; help-dialog focus trap cycles correctly and releases on close; skip links and landmark structure exposed as designed; chat streaming announces via `role="status"` ("Assistant is replying") and a `role="log"` message stream with per-message `<article>` elements and sr-only "You said:"/"Assistant replied:" prefixes; sources disclosure and Stop button appear/disappear correctly around the stream.

**NVDA pass** (NVDA + Microsoft Edge on Windows 11, automation via Guidepup with captured speech; July 10, 2026). All nine user-reachable pages were read with NVDA. What NVDA actually announced:

- *Sign-in page*: form landmark and "Sign in" region; "Email address — edit, required"; "Password — edit, protected, required"; "Show password — check box, not checked"; sign-in/sign-up mode announced as "Authentication mode, grouping, toggle button, pressed"; all buttons named. Credentials could be pasted (no paste blocking) and sign-in was completed entirely through NVDA — supporting 3.3.2, 3.3.8, and 1.3.1 for the auth flow.
- *Landing page*: correct heading outline (level 1 wordmark, level 2 feature sections), labelled navigation/complementary/content-info landmarks, "Skip to main content" link, "(opens in new tab)" announced on partner links.
- *Authenticated home*: h1 announced; the authoring-provided screen-reader orientation note is read; search box announced with label and hint; filter combo boxes announced with labels and values; the div-based grid announced as "table, with 11 rows and 5 columns, Grants" with header-row sort buttons — the ARIA table semantics work in a real screen reader.
- *Home tab-walk*: focus went search → filters → sort buttons → pagination → "Go to Admin Dashboard"; **NVDA never landed on a grant row**, re-confirming the 2.1.1 blocker with real AT.
- *Requirements checklist*: "Requirements navigation" sidebar landmark, h1 "Application Requirements for [NOFO name]", Help button, tabs announced as "tab, selected, ELIGIBILITY".
- *Chat playground (live conversation)*: h1 with the NOFO name; "Grant Well assistant chat, region, list"; the assistant's message read with its sr-only "Assistant replied:" prefix; "Copy message to clipboard" button; "Message the Grant Well assistant" input announced as "edit, multi line"; voice input announced as "toggle button, not pressed, Start voice input".
- *Chat sessions*: sidebar navigation buttons, breadcrumb with "current page, Sessions" (`aria-current`), h1, disabled Delete announced as "unavailable", sessions grid announced as a table.
- *Document editor*: welcome modal content readable (heading levels, step list, "GET STARTED"); wizard step 1 announces the ProgressStepper correctly — "button, current step, Step 1: Project Basics (current)" with future steps "unavailable" (`aria-current="step"` and disabled states verified in real AT).
- *Drafts*: breadcrumb "list, with 2 items … current page, Drafts", h1, "Create new draft" button.
- *Admin dashboard*: h1 "Admin Dashboard", "Grants tab selected" (tab semantics), and a refreshing control announced as "button, unavailable, busy, Refresh dashboard" (`aria-busy` voiced).

Limitations: the staging Processing Review queue was empty ("No NOFOs pending review"), so the quality-score contrast failures (1.4.3) could not be exercised live — the computed ratios from CSS stand as evidence. The maintenance page renders only during maintenance mode and was reviewed in code only. Delete-confirmation and document-upload dialogs, wizard steps 2-5, and the dashboard's non-default tabs were evaluated in code and via the axe/accessibility-tree pass, but not read with NVDA.

## Terms

The terms used in the Conformance Level information are defined as follows:

- **Supports**: The functionality of the product has at least one method that meets the criterion without known defects or meets with equivalent facilitation.
- **Partially Supports**: Some functionality of the product does not meet the criterion.
- **Does Not Support**: The majority of product functionality does not meet the criterion.
- **Not Applicable**: The criterion is not relevant to the product.

---

## Table 1: Success Criteria, Level A

| Criteria | Conformance Level | Remarks and Explanations |
|---|---|---|
| **1.1.1 Non-text Content** | Supports | Every `<img>` carries an `alt` attribute; decorative images use `alt=""` and inline SVGs are `aria-hidden="true"`. A scan of all 196 `<button>` instances found zero icon-only buttons lacking an accessible name; icon buttons consistently pair `aria-hidden` icons with `aria-label` (e.g., `ChatMessage.tsx:496`, `ChatInputPanel.tsx:617-641`). Spinners expose `role="status"` + labels (`LoadingSpinner.tsx:76-106`). |
| **1.2.1 Audio-only and Video-only (Prerecorded)** | Not Applicable | No audio or video content exists in the product (verified: no `<audio>`, `<video>`, `autoplay`, or `Audio()` usage). Microphone input for chat produces no audio output. |
| **1.2.2 Captions (Prerecorded)** | Not Applicable | No prerecorded media. |
| **1.2.3 Audio Description or Media Alternative (Prerecorded)** | Not Applicable | No prerecorded media. |
| **1.3.1 Info and Relationships** | Supports | Most relationships are programmatic: real `<table>` with `<th scope="col">` (`UserManagementTab.tsx:280-287`), complete ARIA grid roles with `aria-sort` on div-based tables, `fieldset`/`legend` on radio groups, `label htmlFor` throughout. **Remediated 2026-07-10**: grant rows are now focusable and keyboard-operable with their selection semantics exposed. Remaining minor advisories (not conformance failures): SessionsPage breadcrumbs are plain divs unlike the `<ol>/<li>` breadcrumbs elsewhere (`SessionsPage.tsx:83-95`); sidebar section titles are styled divs, not headings (`UnifiedNavigation.tsx`). |
| **1.3.2 Meaningful Sequence** | Supports | DOM order matches visual order; the only `row-reverse`/`order` uses are cosmetic (avatar position in `ChatMessage.tsx:45-46`, a nav block at one breakpoint) and preserve meaningful reading order. |
| **1.3.3 Sensory Characteristics** | Supports | Positional wording is always paired with the control's name (e.g., "the 'Chat with AI' button in the navigation panel", `ChecklistPage.tsx:258-262`); `HomePage.tsx:299-307` adds a visually-hidden screen-reader orientation note. |
| **1.4.1 Use of Color** | Supports | Diff views pair color with "+/−" glyphs, hidden "Added:/Removed:" text and strikethrough (`SummaryDiff.tsx:66-79`); statuses use icon + text; required fields use asterisk + `aria-required`. Minor: dashboard metric thresholds (high/medium/low) are conveyed by color only, though the numeric value itself is shown (`ProcessingMetrics.tsx:36-56`). |
| **1.4.2 Audio Control** | Not Applicable | No auto-playing audio. |
| **2.1.1 Keyboard** | Supports | Navigation, menus (arrow-key support), tabs (roving tabindex), tables, modals, and both upload dropzones are keyboard operable. **Remediated 2026-07-10**: grant rows now take keyboard focus and activate on Enter/Space with a visible focus style (verified live: Tab reaches a row, Enter selects the grant and reveals the action buttons); the DocumentManager upload target now opens the file picker on Enter/Space. Remaining minor advisory: the dashboard filter menu lacks Escape/arrow-key handling but is fully operable via Tab (`DashboardPage.tsx`). |
| **2.1.2 No Keyboard Trap** | Supports | The shared modal traps Tab but always releases on Escape (`Modal.tsx:20-29, 86-124`; `hooks/use-focus-trap.ts:64-102`); nested confirm dialogs handle their own Escape; the section editor and chat input are native textareas that Tab exits normally. |
| **2.1.4 Character Key Shortcuts** | Not Applicable | No single-character shortcuts exist; all key handling uses Enter, Escape, Tab, arrows, Home, End, Space (verified by grep of every `e.key ===`). |
| **2.2.1 Timing Adjustable** | Supports | No session/idle timeout exists in the SPA; toasts either never auto-dismiss or pause on hover and focus with a manual dismiss (`DocumentManager.tsx:745-759`). **Remediated 2026-07-10**: the app-imposed 30-second voice-input cutoff is removed — recording continues until the user stops it with the same clearly-indicated toggle (`aria-pressed`, state-aware label), so no app time limit applies. The 60-second chat response timeout remains as a real-time network safeguard (essential exception) and its expiry is now announced. |
| **2.2.2 Pause, Stop, Hide** | Supports | Chat streaming has a labelled Stop control (`ChatInputPanel.tsx:725-733`); a global `prefers-reduced-motion` kill-switch disables animations (`app.scss:29-39`); no carousels/marquees. **Remediated 2026-07-10**: search-tip rotation now pauses on hover/focus, respects `prefers-reduced-motion`, and no longer re-announces via `role="status"`; the dashboard's 10-second auto-refresh during NOFO processing now has a "Pause auto-refresh"/"Resume auto-refresh" toggle (`aria-pressed`) next to the Refresh button (verified live while a NOFO was processing). |
| **2.3.1 Three Flashes or Below Threshold** | Supports | No flashing or blinking content exists (verified by code/CSS scan); the only infinite animations are loading spinners. |
| **2.4.1 Bypass Blocks** | Supports | "Skip to main content" links in both the public and authenticated navbars (`chrome.tsx:101-107, 183-189`), properly hidden until focused, targeting `main#main-content` with `tabIndex={-1}` (`App.tsx:96`). Full landmark set: labelled `<nav>`, `<main>`, `<footer>`, `<aside>`. Minor: page sidebars render inside `<main>`, nesting nav landmarks non-ideally. |
| **2.4.2 Page Titled** | Supports | Static descriptive title in `index.html:28`; per-route `document.title` for all authenticated routes including dynamic ones (`App.tsx:31-61`). Minor: `/login` does not receive a distinct title (falls back to the static title). |
| **2.4.3 Focus Order** | Supports | Modals save and restore invoker focus and focus the first control on open (`Modal.tsx:59-83`); SPA route changes move focus to `<main>` (`App.tsx:24-28`); menus focus their first item on open; the auth panel moves focus to headings/alerts on step changes (`AuthPanel.tsx:511-544`). Minor: dropdown menus restore focus on Escape but drop it to `<body>` after item activation or outside-click close (`GrantActionsDropdown.tsx:39-58`, `RowActions.tsx:20-39`). |
| **2.4.4 Link Purpose (In Context)** | Supports | No "click here"-style links anywhere (verified). Links use content titles or contextual `aria-label`s (`HistoryPanel.tsx:34`, `ResourcesPanel.tsx:18-26`, session titles in `SessionList.tsx:318-346`). Content-dependent caveat: the markdown renderer passes through whatever link text AI-extracted NOFO summaries contain (`ChecklistPage.tsx:241-247`). |
| **2.5.1 Pointer Gestures** | Not Applicable | No multipoint or path-based gestures; no touch/pointer-move handlers exist (verified). Drag-and-drop upload zones have click alternatives (see 2.5.7). |
| **2.5.2 Pointer Cancellation** | Supports | All activation uses standard `onClick` (up event); no down-event activation handlers. Outside-`mousedown` is used only to dismiss menus (abort, permitted). |
| **2.5.3 Label in Name** | Supports | Icon-only buttons have clean `aria-label`s and extended labels usually start with the visible text. **Remediated 2026-07-10**: all three mismatches fixed — the accessible names of the delete-confirmation button, drafts filter toggle, and grants show-more control now begin with their visible text. |
| **2.5.4 Motion Actuation** | Not Applicable | No device-motion or user-motion input (verified: no devicemotion/deviceorientation usage). |
| **3.1.1 Language of Page** | Supports | `<html lang="en">` (`index.html:2`). |
| **3.2.1 On Focus** | Supports | Focus handlers only apply styling or pause a toast timer; none trigger a context change. |
| **3.2.2 On Input** | Supports | No select auto-navigates; filters and page-size selects update in place. Debounced AI search updates results in place with a status region — not a context change; Enter triggers explicitly (`IntegratedSearchBar.tsx:89-139`). |
| **3.2.6 Consistent Help** | Supports | Where help is offered (chat playground, requirements checklist) it is a Help button in the same top-right header position opening a dialog (`PlaygroundPage.tsx:136-144`, `ChecklistPage.tsx:198-200`), auto-opened on first visit. Informative note: no app-wide help/contact mechanism exists on Home/Dashboard/Editor pages (not a violation — the criterion requires consistency only where help is provided). |
| **3.3.1 Error Identification** | Supports | Errors are identified in text and generally announced: form error summary with `role="alert"`, auto-focus, and per-field links (`FormErrorSummary.tsx:133-161`); per-field `aria-invalid` + `aria-describedby` + `role="alert"` in the document editor and dashboard (`ProjectBasics.tsx:133-175`, `ReviewExpandedRow.tsx:413-435`). Minor advisories: auth-step errors appear only in a global live-region alert without `aria-invalid`/`aria-describedby` on the failing input; the feedback form error is not linked to its textarea. |
| **3.3.2 Labels or Instructions** | Supports | No placeholder-only fields anywhere; every input/select/textarea has a programmatic label (visible `htmlFor` pairs, sr-only labels where design hides them, per-row contextual labels in admin tables). Instructions provided where needed: live password-requirements checklist linked via `aria-describedby` (`SignUpStep.tsx:75`), upload format/size limits, required-field legend. |
| **3.3.7 Redundant Entry** | Supports | Auth flow carries the email across sign-in → forgot/reset → verify steps without re-asking (`AuthPanel.tsx:38, 204, 235-239, 298`); the document-editor wizard repopulates previously entered data from the backend draft or localStorage on every revisit and reuses it for draft generation (`ProjectBasics.tsx:244-288`, `UploadDocuments.tsx:302-308`). |
| **4.1.2 Name, Role, Value** | Supports | Strong overall: correct WAI-ARIA tabs (three implementations), menus with `aria-haspopup/expanded` and focus management, dialogs with `role="dialog"`/`aria-modal`/`aria-labelledby`, `aria-sort`, `aria-current`, `aria-busy`, `aria-pressed` toggles, 202 aria-labels codebase-wide. **Remediated 2026-07-10**: grant rows are focusable with keyboard activation; section-sidebar buttons expose completed/failed/generating status in their accessible names and `aria-current` on the active section; the DocumentManager dialog carries `aria-labelledby` on the `role="dialog"` element; chat source lists use unique `useId()`-based IDs with valid group/list/listitem structure. Remaining minor advisory: backgrounds get `aria-hidden` while help dialogs are open but are not made `inert` (`ChecklistPage.tsx`); focus is trapped inside the dialog, mitigating impact. |

---

## Table 2: Success Criteria, Level AA

| Criteria | Conformance Level | Remarks and Explanations |
|---|---|---|
| **1.2.4 Captions (Live)** | Not Applicable | No live media. |
| **1.2.5 Audio Description (Prerecorded)** | Not Applicable | No prerecorded media. |
| **1.3.4 Orientation** | Supports | Responsive layouts throughout; no orientation locks or orientation-dependent functionality (verified: no orientation media queries or Screen Orientation API usage). |
| **1.3.5 Identify Input Purpose** | Supports | Correct `autocomplete` tokens throughout: `email`, `current-password`/`new-password`, `one-time-code` (+ `inputMode="numeric"`) in auth (`SignInStep.tsx:43,60`, `ResetPasswordStep.tsx:47-48`); `organization`, `address-level2`, `postal-code`, `name`, `email` in the project-basics form (`ProjectBasics.tsx:463-537`). |
| **1.4.3 Contrast (Minimum)** | Supports | Core palette passes: body #333 on white = 12.63:1, secondary #5a5a5a = 6.90:1, primary #23776C text/buttons = 5.35:1, placeholders globally forced to #757575 = 4.61:1 (`app.scss:198-227`). **Remediated 2026-07-10**: all failing pairs replaced with compliant colors — quality-score amber now #B45309 (4.52:1), the success token is now #047857 (5.48:1, fixing quality scores, diff-added, "Done", "Saved", and the "New" badge), the muted-text token is now #6b7280 (4.83:1), `.warning-text` now uses the palette danger #CD0D0D (5.74:1), password-met text #047857, archived rows #6b7280. |
| **1.4.4 Resize Text** | Supports | Full-page zoom to 200% works — flex/percentage layouts with breakpoints and no fixed-height text containers that clip. Note: font sizes are ~98% px-based, so browser text-size-only scaling is limited (zoom is the supported mechanism). |
| **1.4.5 Images of Text** | Supports | The only text-as-image instances are logotypes (GrantWell wordmark, partner logos), which are exempt. |
| **1.4.10 Reflow** | Supports | Media queries down to ≤320px in nearly every stylesheet; no min-width on page containers; wide data tables use permitted `overflow-x: auto`. Minor: the feedback info popover's `min-width: 320px` overflows a 320px viewport when open (`landing-page.css:639-649`). |
| **1.4.11 Non-text Contrast** | Supports | Focus indicators are strong: global 2-3px #23776C outlines = 5.35:1 with a dark-background variant (`utilities.css:52-68`). **Remediated 2026-07-10**: input borders now #767676 (4.54:1) in both the shared `inputStyles` and the auth panel (verified live: computed border rgb(118,118,118)); ProgressStepper inactive circles/connectors now #6b7280 (4.83:1). |
| **1.4.12 Text Spacing** | Supports | No fixed-height/`overflow: hidden` multi-line text containers; `white-space: nowrap` is confined to sr-only utilities, single-line cells/badges, and buttons; truncation uses ellipsis. |
| **1.4.13 Content on Hover or Focus** | Supports | The click-toggled feedback popover has a labelled close button and restores focus (exemplary, `FeedbackForm.tsx:160-195`). **Remediated 2026-07-10**: the ProgressStepper tooltip is now dismissible with Escape in addition to being hoverable and persistent. Native `title` tooltips are user-agent controlled (exempt). |
| **2.4.5 Multiple Ways** | Supports | Six routes to content: persistent top nav, sidebar navigation, home search (including AI search with labelled input and help text), filterable/sortable grants table, Recently Viewed panel, and breadcrumbs on interior pages. |
| **2.4.6 Headings and Labels** | Supports | Exactly one descriptive `<h1>` per view with logical h2/h3 flow; labels are descriptive throughout. Minor: markdown headings in chat replies are demoted (h1→h3), which can skip levels; loading/error views start at `<h2>` with no h1 present. |
| **2.4.7 Focus Visible** | Supports | Strong global focus styling: 3px outlines on interactive elements (`app.scss:41-59`), ~40 component-specific `:focus-visible` rules, and every CSS `outline: none` verified to have a visible replacement. **Remediated 2026-07-10**: the inline `outline: "none"` on the chat Send and Stop buttons is removed; the global 3px `:focus-visible` outline now applies (verified live via keyboard tab). |
| **2.4.11 Focus Not Obscured (Minimum)** | Supports | Sticky elements reviewed (table headers, side navigation, edge feedback tab) cannot fully hide a focused element; no fixed full-width overlays over scrolling content. |
| **2.5.7 Dragging Movements** | Supports | Both drag-and-drop upload zones offer single-pointer click alternatives (`DocumentManager.tsx:825-850`, `UploadDocuments.tsx:586-630`); no other draggable interactions exist. |
| **2.5.8 Target Size (Minimum)** | Supports | 63 CSS declarations enforce 44px minimum targets; chat controls are ≥44px, copy/toggle buttons ≥32px. **Remediated 2026-07-10**: checkboxes enlarged to 20px (their surrounding spacing satisfies the 24px-spacing exception); the shared modal close button now has a 44px minimum target; session-list sort headers received padding. |
| **3.1.2 Language of Parts** | Not Applicable | All UI text is English; no passages in other languages ship. (If non-English NOFO content is ever ingested, the markdown renderer would need `lang` handling.) |
| **3.2.3 Consistent Navigation** | Supports | Global chrome (header, navbar, footer) rendered once for every authenticated route in the same order (`AppConfigured.tsx:207-220`); the sidebar is a single shared component with fixed item order on all tool pages. |
| **3.2.4 Consistent Identification** | Supports | Consistent icon+label pairs (trash=delete, download, X=close), skip links, pagination, and modal-close labeling across pages. Minor: the identical sidebar is `aria-label`ed three different ways across pages ("Application navigation" / "Requirements navigation" / "Document editor navigation"). |
| **3.3.3 Error Suggestion** | Supports | Specific corrective messages throughout: field-level format/range suggestions (`ProjectBasics.tsx:302-346`), Cognito errors mapped to actionable text (`auth-utils.ts:160-208`), per-file upload rejection reasons with limits (`UploadDocuments.tsx:44-60`). |
| **3.3.4 Error Prevention (Legal, Financial, Data)** | Supports | Every destructive action is confirmed: shared delete-confirmation modal naming the items with a "cannot be undone" warning; user deletion names the account; NOFO rejection requires typed justification plus a second confirmation echoing the reason (`ReviewExpandedRow.tsx:407-486`). Wizard data is auto-saved and reviewable/editable before submission. |
| **3.3.8 Accessible Authentication (Minimum)** | Supports | No CAPTCHA or cognitive test in any auth flow; paste is never blocked (zero `onPaste` handlers); password managers and OS code autofill fully supported via `current-password`/`new-password`/`one-time-code` tokens; show-password toggles on every password field. |
| **4.1.3 Status Messages** | Supports | Extensive working live regions: chat streaming announced via `role="log"` (`Chat.tsx:278`) plus an sr-only status region; autosave indicator, upload progress, search-result counts, tab-change announcements, generation progress, and error banners all use `role="status"`/`role="alert"` appropriately. **Remediated 2026-07-10**: `NotificationProvider`, `NotificationBar`, and `AccessDeniedProvider` are now mounted at the app root (verified live: the dashboard Refresh success toast renders and is announced); errors/warnings announce via `role="alert"` and info/success politely via `role="status"`; section-save feedback now uses React state plus a visually-hidden live region; the session-list, processing-review, and review-detail loading states carry `role="status"` and the checklist error state `role="alert"`; the 60-second chat timeout message now renders in the message history. |

---

## Summary of Results

Post-remediation (source tree of July 10, 2026; pre-remediation counts in parentheses):

| Conformance Level | Level A (31 criteria) | Level AA (24 criteria) |
|---|---|---|
| Supports | 24 | 21 |
| Partially Supports | 0 | 0 |
| Does Not Support | 0 | 0 |
| Not Applicable | 7 | 3 |

After remediation, every applicable Level A and AA criterion is rated **Supports** (some with minor advisories noted in the remarks).

## Remediation Priorities — Status

All items below — plus the two follow-up timing fixes (2.2.1 voice-input limit removed; 2.2.2 dashboard auto-refresh pause toggle) — were **applied on July 10, 2026** and verified against a local build (see Note 6). Ordered by user impact; file references are the primary fix sites.

1. ✅ **FIXED** — **Grant selection is mouse-only** (2.1.1, 4.1.2, 1.3.1 — blocker). Make the grant rows focusable and activatable (`tabIndex={0}`, Enter/Space handling, interactive role) or make the grant-name cell a real button/link, matching the pattern already used in `DocumentManager.tsx:1019` — `pages/home/GrantsTable.tsx:464-476`.
2. ✅ **FIXED** — **Mount the notification system at the app root** (4.1.3 — blocker; also a functional bug for all users). Move `NotificationProvider`, `NotificationBar`, and `AccessDeniedProvider` from the unused `BaseAppLayout` into `App.tsx`/`AppConfigured.tsx`.
3. ✅ **FIXED** — **Fix failing text colors** (1.4.3 — blocker/major). Replace #F6B622 (1.81:1), #24A850 (3.09:1), #9ca3af (2.54:1), and #e74c3c (3.82:1) where used as text with the compliant equivalents already in the palette (#047857, #1b5d54, #5a5a5a).
4. ✅ **FIXED** — **Restore visible focus on chat Send/Stop** (2.4.7 — major). Remove the inline `outline: "none"` — `components/chat/ChatInputPanel.tsx:114, 134`.
5. ✅ **FIXED** — **Make the DocumentManager upload keyboard-activatable** (2.1.1 — major). Add Enter/Space handling on the focusable label or use the visually-hidden-input pattern from `UploadDocuments.tsx:586-630` — `components/chat/DocumentManager.tsx:834`.
6. ✅ **FIXED** — **Darken input borders** (1.4.11 — major). Raise field-border tokens from #e2e8f0/#d7dee8 to ≥3:1 against white (e.g., #949494 or the existing #23776C family) — `components/ui/styles.ts:42`, `styles/auth-panel.css:64-81`.
7. ✅ **FIXED** — **Align accessible names with visible text** (2.5.3 — major). Ensure `aria-label` contains the visible string on the shared delete-confirm button, drafts filter toggle, and grants show-more control — `DeleteConfirmationModal.tsx:67-72`, `DocEditorSessions.tsx:258-265`, `GrantsTable.tsx:549-560`.
8. ✅ **FIXED** — **Announce section-save results via a live region** (4.1.3 — major). Replace the `innerText` mutation with the existing `AutoSaveIndicator` pattern — `pages/document-editor/SectionsEditor.tsx:309-326`.
9. ✅ **FIXED** — Smaller items: `aria-current` + status exposure in `SectionsSidebar.tsx:77-89`; `role="status"`/`role="alert"` on session-list/review/checklist loading and error states; de-duplicate `id="source-list"` and fix nested `listitem`s in `ChatMessage.tsx:352-454`; pause control for rotating search tips (`IntegratedSearchBar.tsx:145-175`); distinct `/login` page title; 24px targets for checkboxes and the modal close button.

---

*"Voluntary Product Accessibility Template" and "VPAT" are registered service marks of the Information Technology Industry Council (ITI). This report uses the VPAT WCAG edition structure; verify current template language at https://www.itic.org/policy/accessibility/vpat before external publication.*
